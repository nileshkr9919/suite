import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  imports: [CommonModule],
  selector: 'app-notes-entry',
  template: `Notes App Remote Entry`,
})
export class RemoteEntry {}
